interface:
  display_name: "11agi Benchmarks Machine"
  short_description: "Analyze global LLM cost, effort, and time"
  default_prompt: "Use $11agi-ai-analytics-machine to analyze machine-wide LLM usage, harness surfaces, billing modes, cost by model and effort, wall time, and estimated active time, then create matching Markdown and HTML reports."
