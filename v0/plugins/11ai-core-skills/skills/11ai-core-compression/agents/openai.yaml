interface:
  display_name: "11agi Core Compression"
  short_description: "Compress markdown guidance without external CLI"
  default_prompt: "Use $11agi-core-compression to compress a markdown guidance file locally while preserving headings, code, links, and paths."
