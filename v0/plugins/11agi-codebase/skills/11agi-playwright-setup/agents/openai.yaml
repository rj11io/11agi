interface:
  display_name: "11agi Playwright Setup"
  short_description: "Set up Playwright tests and CI"
  default_prompt: "Use $11agi-playwright-setup to add Playwright unit and E2E tests with CI browser setup and mock test env vars."
