interface:
  display_name: "11agi Audit Security Dependency Scan"
  short_description: "Gate every dependency version against vulns and malware"
  default_prompt: "Use $11agi-audit-sec-deps-scripts to enumerate every dependency version this project could install and check each candidate against current vulnerability, malware, registry, and web evidence before approving any install."
