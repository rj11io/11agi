interface:
  display_name: "11agi Git Main Director"
  short_description: "Run a task safely on the main branch with Git discipline"
  default_prompt: "Use $11agi-director-git-main to synchronize this repository's main branch, perform the requested task, quality-check it, and report everything done without committing unless I ask."
